{
  "name": "@aibuilder/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Hono Worker behind the onboarding modal: drafts, uploads, slug/geo, submit, SSE proxy and claim.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@aibuilder/core": "workspace:*",
    "@aibuilder/db": "workspace:*",
    "@aibuilder/site-schema": "workspace:*",
    "aws4fetch": "^1.0.20",
    "hono": "^4.13.7",
    "ulid": "^3.0.2",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260906.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.129.0"
  }
}
