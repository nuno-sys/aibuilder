{
  "name": "@aibuilder/renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The tenant read path: serves *.mijnsaas.com and every verified custom hostname from KV, the edge cache and R2. Holds no D1 binding and no secret.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@aibuilder/core": "workspace:*",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260906.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.129.0"
  }
}
