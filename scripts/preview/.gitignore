# Generated by run.mjs: the esbuild output for each entry point. Named `dist/` because the root
# .gitignore and the root eslint config both already ignore that name everywhere.
dist/

# `npm install` inside this directory (playwright, the four font families, @types/node).
# The root .gitignore already ignores `node_modules/` at any depth; restated here so this
# directory is self-explanatory when read on its own.
node_modules/
package-lock.json
